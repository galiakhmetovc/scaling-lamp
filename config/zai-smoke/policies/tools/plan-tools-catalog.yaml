kind: PlanToolPolicyConfig
version: v1
id: plan-tools-zai-smoke
spec:
  enabled: true
  strategy: default_plan_tools
  params:
    tool_ids:
      - init_plan
      - add_task
      - set_task_status
      - add_task_note
      - edit_task
      - plan_snapshot
      - plan_lint
