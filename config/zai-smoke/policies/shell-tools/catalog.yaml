kind: ShellCatalogPolicyConfig
version: v1
id: shell-catalog-zai-smoke
spec:
  enabled: true
  strategy: static_allowlist
  params:
    tool_ids:
      - shell_exec
      - shell_start
      - shell_poll
      - shell_kill
    allow_empty: false
