kind: ToolAccessPolicyConfig
version: v1
id: tool-access-zai-smoke
spec:
  enabled: true
  strategy: static_allowlist
  params:
    tool_ids:
      - init_plan
      - add_task
      - set_task_status
      - add_task_note
      - edit_task
